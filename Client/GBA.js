import React from 'react';
import ReactDOM from 'react-dom';
import jquery from 'jquery';
import { Layout } from './js/React Components/GBA Layout';

const app = document.getElementById('app');

ReactDOM.render(
  <Layout />,
  app);
